import { TransferFilterPipe } from './transfer-filter.pipe';

describe('TransferFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
